# --- Operating System Files ---
# Ignore files generated by macOS, Windows, Linux environments
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db
*.stackdump

# --- IDEs / Editors ---
# Ignore configuration files and workspace settings for common editors
.vscode/*
!.vscode/extensions.json # Optional: Commit recommended extensions
!.vscode/settings.json   # Optional: Commit shared workspace settings (be careful with paths)
.idea/
*~ # Ignore editor backup files

# --- Logs ---
# Ignore log files
*.log
logs/
*.log.*

# --- Docker Compose Specific ---

# Ignore directories used for testing directories
mnt/
test/

# Ignore default environment file(s) if they contain secrets or local paths.
# Only commit .env if it contains non-secret, environment-agnostic defaults.
# !.env

# Ignore local override environment files (DO NOT COMMIT THESE)
dev.env # Your specific development override file

# Ignore local Docker Compose override figitles (unless used for shared dev config)
docker-compose.override.yml
docker-compose.override.yaml

# --- Secrets / Sensitive Files ---
# Ignore any files potentially containing sensitive information
*.pem
*.key
credentials.*
secrets.*
*.token
*.secret
**/.secrets.*
